framework:
    messenger:
        transports:
            sync:
                dsn: 'sync://'
            async:
                dsn: "%env(MESSENGER_TRANSPORT_DSN)%"

when@test:
    framework:
        messenger:
            transports:
                async: 'in-memory://'

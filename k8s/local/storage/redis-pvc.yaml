apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: redis-pvc
    namespace: shop-system
spec:
    storageClassName: standard
    accessModes:
        - ReadWriteOnce
    resources:
        requests:
            storage: 1Gi

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: elasticsearch-pvc
    namespace: shop-system
spec:
    storageClassName: standard
    accessModes:
        - ReadWriteOnce
    resources:
        requests:
            storage: 5Gi
